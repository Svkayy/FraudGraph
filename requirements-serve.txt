# Local serving + monitoring stack (Stages 5-6). Runs on your Mac in .venv.
# Heavy training deps (xgboost, full PyG training) live on Kaggle, not here.

fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2

redis==5.0.8
kafka-python-ng>=2.2  # maintained fork; supports Python 3.12+; import name stays `kafka`

# Inference-only: CPU torch + PyG. Versions kept broad for arm64 wheels.
torch>=2.2,<2.7
torch-geometric>=2.5,<2.7

numpy>=1.26,<2.1
pandas>=2.1
prometheus-client==0.21.0
requests==2.32.3
